<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Retab\Resource;

/**
 * Request payload for ``POST /workflows/{id}/diagnose-graph``.
 *
 * ``blocks`` and ``edges`` are both nullable so the route can tell apart
 * two intents:
 *
 * * ``blocks`` / ``edges`` omitted (``None``): diagnose the persisted draft
 *   — the route loads blocks and edges from MongoDB.
 * * ``blocks`` / ``edges`` provided (even as ``[]``): diagnose the in-memory
 *   graph as-is — used by editor previews and ``workflows spec validate``
 *   against a stub file.
 *
 * Sending only one side (e.g. ``blocks=[]`` without ``edges``) is treated as
 * explicit-empty for the missing side too; the persisted-draft fallback only
 * kicks in when both are omitted.
 */
readonly class WorkflowGraphDiagnosisRequest implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /**
         * Blocks to diagnose; if omitted, the persisted draft is loaded server-side
         * @var array<\Retab\Resource\WorkflowConfigBlock>|null
         */
        public ?array $blocks = null,
        /**
         * Edges to diagnose; if omitted, the persisted draft is loaded server-side
         * @var array<\Retab\Resource\WorkflowConfigEdge>|null
         */
        public ?array $edges = null,
        /** Recompute derived schemas before validating the graph */
        public ?bool $rePropagate = null,
    ) {
    }

    /** @param array<string, mixed> $data */
    public static function fromArray(array $data): self
    {
        return new self(
            blocks: isset($data['blocks']) ? array_map(fn ($item) => WorkflowConfigBlock::fromArray($item), $data['blocks']) : null,
            edges: isset($data['edges']) ? array_map(fn ($item) => WorkflowConfigEdge::fromArray($item), $data['edges']) : null,
            rePropagate: $data['re_propagate'] ?? null,
        );
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'blocks' => $this->blocks !== null ? array_map(fn ($item) => $item->toArray(), $this->blocks) : null,
            'edges' => $this->edges !== null ? array_map(fn ($item) => $item->toArray(), $this->edges) : null,
            're_propagate' => $this->rePropagate,
        ];
    }
}
